import CodeBlock from "@theme/CodeBlock";

export const Call = ({ code, language = "rust" }) => {
  return (
  <>
    <p>
      This is useful if you want to:
    </p>

    <ul>
      <li>Fetch or modify data in other smart contracts</li>
      <li>Interact with standard canisters like ledger or governance</li>
      <li>Trigger behavior on other dapps</li>
    </ul>

    <p>Here’s an example that calls another canister’s method and logs the result:</p>

    <CodeBlock language={language}>{code}</CodeBlock>

  </>
  );
}
